int init(){
    int a, b, i, aux, limite;
    a = 0;
    b = 1;

    print "Digite um número limite: ";
    input limite;

    i = 0;

    while(i < limite){
        aux = a + b;
        a = b;
        b = aux;
        print "%d, ", auxiliar;
        i = i + 1;
    }

    return 0;
}
